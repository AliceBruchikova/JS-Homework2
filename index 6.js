let start = +prompt("Введите начальное число");
let end = +prompt("Введите конечное число");
let sum;
for (let i = start; i <= end; i++) {
    sum = sum(i);     //не знаю как посчитать сумму числового ряда
    alert("Результат: " + sum);
}